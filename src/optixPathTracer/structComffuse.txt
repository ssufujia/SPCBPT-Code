function connect(vertex a,vertex b)
{
    阴影射线判断可见性
    构成完整路径的贡献值
    计算PDF，a->b以及b->a的pdf————这个过程中各个顶点的入射方向的信息似乎是必须的
    计算另一个pdf，即已知连接方向之后，反向生成a以及b的上个顶点的pdf，这个过程中上个顶点的位置属性似乎也是必须的？
        ==存上个顶点位置的话，入射方向就不用存了
    根据pdf带入函数，计算MIS值，理论上来说
}不过乘法除法可以交换，所以依然可以只留一个特殊的D的表述形式
返回两条子路径组成的完整路径的评估值